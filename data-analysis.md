# 数据分析 · 单机性能基准、问题基线与落地差距

> 资产类型：论文数据分析方法
> 边界：论文实验数据可公开；所有性能结果均明确标注为 Python 单机模拟。

## 一、性能基准实验

### 实验纪律

- 固定 CPU、内存、Python 与依赖版本等环境元数据；
- 同一操作重复计时并记录分布，避免只报告单次最优值；
- 将“密码学与交易验证开销”与“分布式网络端到端性能”分开；
- 不从单机耗时直接推导生产吞吐量或完整用户体验。

### 可公开核心结果

| 验证路径 | 单笔耗时 | 能回答的问题 | 不能回答的问题 |
|---|---:|---|---|
| 联盟链单笔交易验证 | **0.173ms** | 基础验证路径在单机原型中的计算开销 | 真实多节点共识、网络和存储延迟 |
| 含零知识证明的验证 | **0.583ms** | 隐私证明加入后的相对增量 | 完整扫码接口、移动网络或生产并发性能 |

加入零知识证明后，单机验证耗时增加约 `0.410ms`。论文据此只能得出“隐私验证在该原型环境中具有可测试的计算开销”，不能写成“生产系统已达到亚毫秒体验”。

## 二、传统跨机构对账的问题基线

论文引用的传统流程基线为：**约 620 万元成本、2–3 年周期**。这两个数字描述现有跨机构对账的成本与时间问题，不是区块链方案的年度节省额，也不是投资回收期。

| 基线 | 论文中的用途 | 表述边界 |
|---|---|---|
| 约 620 万元 | 说明传统跨机构对账的成本压力 | 不改写为“年省 620 万” |
| 2–3 年 | 说明传统对账周期长 | 不改写为“项目回收期 2–3 年” |

区块链方案的经济价值仍需结合节点建设、治理、密钥托管、系统集成和持续运维成本另行评估。本资产库不在缺少完整成本模型时给出 ROI 结论。

## 三、从原型到生产的差距

| 原型已验证 | 生产前仍需验证 |
|---|---|
| 哈希链、背书、Merkle 证明与范围证明的逻辑 | 多节点网络、共识、故障和容量压力 |
| 状态机能拒绝非法跳转 | 多机构治理、权限变更和异常追责 |
| 单机验证路径可计时 | 密钥管理、托管、接口与真实设备性能 |
| 隐私证明不直接暴露原始数值 | 数据授权、个人信息保护与合规审查 |

## 四、可迁移方法

1. **性能数据必须贴着环境声明**：单机结果不外推生产网络；
2. **问题基线不等于方案收益**：现有成本不能直接写成新方案节省；
3. **技术评估回答三件事**：原型验证了什么、还缺什么、下一步如何验证；
4. **主动降低结论强度**：比制造更大的数字更能建立技术判断力。
