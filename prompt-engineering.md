# Prompt 工程 · AI 辅助科研的 Prompt 实践

> 资产类型：AI 辅助科研 Prompt 库与使用纪律
> 用途：科研场景的 AI 使用与产品场景截然不同——错误代价高（引用造假、密码学实现漏洞、数据口径错误）。本文沉淀"哪些环节敢用 AI、怎么用、如何验证"的实践。

## 一、总原则：AI 产出三级信任分级

| 级别 | 环节 | 使用方式 | 验证要求 |
|---|---|---|---|
| 可直接用 | 格式转换、图表脚本、LaTeX 排版 | AI 全权生成 | 目测结果 |
| 需抽查 | 文献速读摘要、代码骨架、表达润色 | AI 初稿 + 人工抽查 | 关键点回查原文/逐行读核心逻辑 |
| 仅作参谋 | 密码学协议实现、引用列表、实验结论表述 | AI 提建议，人工执笔 | 全量人工验证 |

**风险处理**：挑战值与随机数生成是 Schnorr 类协议实现的高风险位置，即使功能测试通过也不能据此判断安全。使用规则是：**AI 可辅助测试和骨架，协议核心逻辑必须对照原始论文与安全假设逐行检查。**

## 二、高频 Prompt 模式（节选）

### 模式 1 · 文献对比表生成

```
当前任务研究定制化生产场景的区块链溯源方案。请阅读以下 3 篇论文的摘要
与方法部分（附原文），输出对比表：

列：核心机制 | 信任模型 | 链上存储量 | 验证复杂度 | 隐私能力 | 对当前场景的适配缺口
行：每篇论文一行

要求：每个单元格必须能定位到原文段落（标注章节号），
不确定的写"原文未明确"，禁止推测。
```

**要点**：`禁止推测 + 标注出处`是文献类 Prompt 的标配——AI 的文献幻觉率高，输出必须可回查。

### 模式 2 · 实验代码骨架生成

```
为以下实验编写 Python 模块骨架：
【实验目的】验证 Merkle 树成员证明的验证耗时与树规模的关系
【固定接口】build_tree(records) / gen_proof(idx) / verify(root, proof, leaf)
【基准协议】每个规模点跑 1000 轮，输出 CSV：columns=[op, n, round, ms]
【禁止】不要实现哈希函数本身（用 hashlib），不要用第三方 Merkle 库（自研是实验对象）

先输出模块结构和每个函数的 docstring，等待确认后再写实现。
```

**要点**：`先结构后实现`的两段式生成（与文生应用 Agent 的“计划确认节点”同一思想）；固定接口和基准协议由任务负责人定义，AI 只填充实现。

### 模式 3 · 论文逻辑断层检查

```
以下是论文第 X 章草稿。请扮演严格的审稿人，只做一件事：
找出"主张与证据不匹配"的位置——
1. 有主张但没有实验/引用支撑的句子；
2. 实验结论被过度概括的句子（如单机模拟结果被表述为生产环境性能）；
3. 与前文数据矛盾的表述。
逐条输出：原句 | 问题类型 | 建议修改方向。不要改写文风。
```

**要点**：把 AI 约束在"找断层"单一任务上，比笼统要求"修改论文"更有效；该 Prompt 用于检查"单机模拟 vs 生产环境"的表述边界。

### 模式 4 · 受控润色

```
润色以下段落的英文学术表达。约束：
1. 不改变任何数字、术语和主张强度（may/can/prove 的层级不可改变）；
2. 输出 diff 形式（原句→新句），每处改动附一句理由；
3. 不确定是否改变原意的地方，标记 [CHECK] 交由人工决定。
```

**要点**：`主张强度不可改变`是学术润色的红线——AI 润色最常见的破坏是把 "may improve" 悄悄升级成 "improves"。

## 三、与产品场景 Prompt 工程的方法论对照

| 原则 | 科研场景落地 | 产品场景落地（本资产库其他产品） |
|---|---|---|
| 输出可验证 | 标注出处、diff 形式、[CHECK] 标记 | used_claims 回查（受控配方系统）、JSON Schema（网页采集 Agent） |
| 两段式生成 | 先骨架确认再实现 | 计划确认节点（文生应用 Agent） |
| 负面清单 | 禁止推测、禁止改主张强度 | 禁用词表、禁止越界配比（受控配方系统） |
| 信任分级 | 三级信任分级表 | 高/低置信度分流兜底（各项目通用） |

**结论**：Prompt 工程的本质不是话术，而是**为 AI 的不可靠性设计验证与兜底结构**——这一认知在科研和产品两侧完全通用。
