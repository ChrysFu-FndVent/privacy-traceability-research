# Skills · 6 个 Python 实验模块的工程化拆解

> 资产类型：可复现实验框架
> 用途：论文的每个设计主张都由一个独立 Python 模块验证。模块间低耦合、可单独运行、输出统一格式的实验数据——这套"实验即代码"的组织方式本身就是可复用资产。

## 实验框架结构

```
experiments/
├── 01_hash_chain/          # P1：哈希链完整性验证
├── 02_ecdsa_signature/     # P2：多方背书签名与验签
├── 03_merkle_tree/         # P3：Merkle 树轻验证与数据缩减
├── 04_smart_contract_sim/  # P4：智能合约状态机模拟
├── 05_zkp_range_proof/     # P5：零知识范围证明
├── 06_performance_test/    # 端到端性能基准
├── 07_visualization/       # 实验图表生成
├── 08_graphviz/            # 架构图/状态机图生成
└── 09_data/                # 统一实验数据输出
```

## 模块 1 · 哈希链完整性（01_hash_chain）

- **验证主张**：任意生产记录被篡改后可被检出（P1）
- **实现要点**：批记录序列化 → SHA-256 链式哈希（每条记录哈希包含前驱哈希）；篡改注入实验：随机修改第 k 条记录的任一字段，验证第 k 条之后所有哈希失配
- **实验结论**：篡改会导致后续链式哈希失配；本模块验证检出逻辑，不在公开版本中披露未进入事实底稿的性能数字。

## 模块 2 · ECDSA 多方背书（02_ecdsa_signature）

- **验证主张**：≥2 方独立签名的记录不可由单方伪造（P2）
- **实现要点**：secp256k1 曲线密钥对生成；背书流程模拟：生产方签名 → 质检方复签 → 背书数校验（MIN_CERTIFICATIONS=2）；伪造实验：单签/错签/重放记录均被拒绝
- **实验结论**：单方签名不足、错误签名与重放记录会被拒绝；具体单机性能统一在模块 6 按完整验证路径报告。

## 模块 3 · Merkle 树轻验证（03_merkle_tree）

- **验证主张**：只上链 Merkle 根即可支持任意单条记录的成员证明（P3）
- **实现要点**：批内全量记录构建 Merkle 树 → 链上仅存根哈希；消费者验证单瓶记录只需 log(n) 条路径哈希；数据缩减测算：全量上链 vs 根锚定的存储对比
- **实验结论**：成员证明可以在不读取全量记录的情况下验证单条记录；不把该原语结果外推为完整扫码体验。

## 模块 4 · 智能合约状态机（04_smart_contract_sim）

- **验证主张**：非法状态跳转在合约层被拒绝（P4）
- **实现要点**：8 状态状态机（RAW_MATERIAL_REGISTERED → PRODUCTION_STARTED → … → SOLD / RECALLED）；RBAC 7 角色权限矩阵；5 个核心接口（registerProduct / addTraceNode / endorseProduct / verifyAuthenticity / freezeProduct）；非法跳转穷举测试（如未质检直接售出）
- **实验结论**：全部非法跳转与越权调用被正确拒绝；状态机穷举测试用例本身构成合约的"验收规格"

## 模块 5 · 零知识范围证明（05_zkp_range_proof）

- **验证主张**：可证明"肤质参数在合规区间内"而不泄露数值（P5）
- **实现要点**：Pedersen 承诺（值隐藏 + 绑定）；Schnorr 协议知识证明；Chaum-Pedersen OR 证明组合实现区间判定；隐私推断实验：验证者视角不可区分不同承诺值
- **实验结论**：加入零知识证明后的单机验证路径耗时为 0.583ms；结果只用于原型环境的相对开销比较。

## 模块 6 · 端到端性能基准（06_performance_test）

- **验证主张**：比较基础联盟链交易验证与加入零知识证明后的单机计算开销（P6）
- **实现要点**：统一基准协议（每项操作 N 轮取均值、报告 P50/P99）；四重校验流程串联计时（哈希校验 → 签名校验 → Merkle 成员证明 → 状态校验）；输出 CSV 到 09_data，07 模块统一出图
- **实验结论**：基础交易验证 0.173ms；加入零知识证明后的验证为 0.583ms。两者均为单机模拟，不代表端到端生产性能。

## 实验工程化的三条规范（可迁移）

1. **一个主张一个模块**：实验模块与论文主张一一对应，审稿人/读者可按图索骥复现任意结论；
2. **统一输出协议**：所有模块输出同构 CSV（操作、轮次、耗时、环境元数据），可视化与统计脚本零适配；
3. **负例实验必做**：篡改注入、伪造签名、非法跳转——"系统能拒绝什么"与"系统能做什么"同等重要（与产品工作中的异常流设计同源）。
